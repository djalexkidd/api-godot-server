<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/admin.css">
    <title>Panneau d'administration</title>
</head>
<body>
    <h1>Panneau d'administration</h1>

    <a href="admin/create/">Ajouter un jeu</a>

    <a href="logout/">Déconnexion</a>

    <table>
        <thead>
            <tr>
                <th>Nom</td>
                <th>Indentifiant</td>
                <th>Auteur</td>
                <th>Clé d'API</td>
                <th>Actions</td>
            </tr>
        </thead>
        <tbody>
            <% for(let i = 0; i < games.length; i++) { %>
            <tr>
                <td data-label="Nom"><%= games[i].display_name %></td>
                <td data-label="ID"><%= games[i].name %></td>
                <td data-label="Auteur"><%= games[i].author %></td>
                <td data-label="Clé d'API"><%= games[i].apikey %></td>
                <td data-label="Actions"><a title="Voir" href="web/<%= games[i].name %>">📜</a><a title="Éditer" href="admin/edit/<%= games[i].name %>">✏️</a><a title="Réinitialiser" href="admin/reset/<%= games[i].name %>" onclick="return confirm('⚠️ Voulez-vous vraiment réinitialiser le classement de ce jeu ?')">🔄</a><a title="Supprimer" href="#" onclick="return deleteLeaderboard('<%= games[i].name %>')">❌</a></td>
            </tr>
            <% } %>
        </tbody>
    </table>

    <script src="/js/admin.js"></script>
</body>
</html>